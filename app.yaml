runtime: python311

env: flex

entrypoint: gunicorn --bind :$PORT --workers 1 --threads 8 --timeout 600 backend.main:app

runtime_config:
  python_version: 3.11

manual_scaling:
  instances: 1

resources:
  cpu: 1
  memory_gb: 2
  disk_size_gb: 10

env_variables:
  PORT: 8080
